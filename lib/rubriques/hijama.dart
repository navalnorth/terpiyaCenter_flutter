import 'package:flutter/material.dart';

class Hijama extends StatelessWidget {
  const Hijama({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('hijama'),
      ),
    );
  }
}